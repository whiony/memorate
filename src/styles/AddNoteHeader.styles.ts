import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    header: {
        alignItems: 'center',
        paddingVertical: 16,
        backgroundColor: '#4C4F6D',
    },
    headerTitle: {
        fontSize: 20,
        fontWeight: 'bold',
        color: '#fff',
    },
});
