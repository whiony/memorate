import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    saveButton: {
        marginTop: 16,
        marginHorizontal: 16,
        backgroundColor: '#4C4F6D',
        borderRadius: 10,
        paddingVertical: 14,
        alignItems: 'center',
    },
    saveButtonText: {
        color: '#fff',
        fontSize: 17,
        fontWeight: '600',
    },
});
