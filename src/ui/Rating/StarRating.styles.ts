import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    section: {
        marginBottom: 6,
        marginTop: 14,
    },
    starContainer: {
        flexDirection: 'row',
    },
    cardStarContainer: {
        flexDirection: 'row',
        alignItems: 'center',
    },
})
